server_names_hash_bucket_size 256;

server {
        listen        80;
        server_name   {{ ec2_instance_public_dns }} "";

        root /var/www/html;
        index index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }
}